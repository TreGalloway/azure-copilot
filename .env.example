# Azure Authentication Configuration
# Choose ONE authentication method below:

# Method 1: Use Azure CLI authentication (RECOMMENDED for local development)
# Just run: az login
# No additional environment variables needed - DefaultAzureCredential will use Azure CLI

# Method 2: Service Principal Authentication (for CI/CD and production)
# Uncomment and fill in these values:
# AZURE_CLIENT_ID=your-service-principal-client-id
# AZURE_CLIENT_SECRET=your-service-principal-secret
# AZURE_TENANT_ID=your-azure-tenant-id

# Method 3: Managed Identity (for Azure VMs/App Service)
# No configuration needed - DefaultAzureCredential will auto-detect

# ============================================================================
# Required: Azure Subscription
# ============================================================================
# Your Azure subscription ID (required for all operations)
AZURE_SUBSCRIPTION_ID=your-subscription-id-here

# ============================================================================
# Optional: Default Settings
# ============================================================================
# Default Azure region for resource creation
DEFAULT_LOCATION=eastus

# Default resource group (if not specified in commands)
DEFAULT_RESOURCE_GROUP=my-default-rg

# ============================================================================
# Week 3+: Azure OpenAI Configuration (Not needed yet)
# ============================================================================
# Uncomment when you reach Week 3 LLM integration:
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-api-key-here
# AZURE_OPENAI_DEPLOYMENT=gpt-4-turbo
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ============================================================================
# Week 5+: Vector Database Configuration (Not needed yet)
# ============================================================================
# CHROMA_PERSIST_DIRECTORY=./data/chroma
# EMBEDDING_MODEL=text-embedding-ada-002

# ============================================================================
# Application Settings
# ============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Enable dry-run mode by default (safer for testing)
# DEFAULT_DRY_RUN=true

# Disable confirmation prompts (use with caution!)
# SKIP_CONFIRMATIONS=false

# ============================================================================
# Development Settings
# ============================================================================
# Enable debug mode for verbose output
# DEBUG=false

# Token usage tracking (for LLM features in Week 3+)
# TRACK_TOKEN_USAGE=true

# ============================================================================
# How to use this file:
# ============================================================================
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in your Azure subscription ID (required)
#
# 3. Choose an authentication method:
#    - For local dev: Just run 'az login' (easiest)
#    - For CI/CD: Uncomment and fill in service principal values
#
# 4. Adjust optional settings as needed
#
# 5. NEVER commit your .env file to git!
#    (It's already in .gitignore)
